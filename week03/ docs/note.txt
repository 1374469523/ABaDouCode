pydantic详细讲下吧
> https://pydantic.dev/docs/validation/latest/get-started/
> 搭配大模型使用， 本身也可以数据格式 （参数格式，请求格式）

Tool里用函数，可以调用。这里写成一个类，一般在什么场景会使用？
> 如果输出格式 不是 text ，是一个可解析的格式，就可以使用

关键词识别就是实体识别吗？
> 关键词是识别关键 / 命名实体识别
> 我今天吃了肠粉，导致拉肚子了。 -》 肠粉 拉肚子
> 我今天吃了肠粉，导致拉肚子了。 肠粉 / 食物 ， 拉肚子 / 身体症状
> 我今天和小王以及小强去奥体吃饭，吃了大米先生。 小王 小强 / 人名； 奥体 / 地理位置 ； 大米先生 / 饭店名字

能讲讲为什么要用tools吗？哪些情况要用tools，哪些情况不用tools吗？
> tool 可以用来进行格式化解析 或 选择调用工具
> 单纯的文本对比，不需要

会介绍Graph Engineering吗
> 后序展开

刚刚作业的思路没太理解，可以再总结一下吗
> pydantic model  -》tool json

迁移学习和泛化能力能等价吗？
> 不等价；迁移学习 是 模型的训练过程， 训练方式； 泛化能力 是 模型在没有见过的样本，测试集的表现；

怎么知道要搬多少层？
> 人工选择。是超参数

最后一层是不是一般是个全连接层，10个神经元变成1个神经元（10分类，到2分类）
> 对的

迁移学习有点像拿出公共组建 改吧改吧一起用
> 类似，拿来主义，复用已有的模型

第一个任务训练的模型本身不能实现第二个任务？
> 是的，在传统深度学习中，数据集不同得到的模型不同

特征蒸馏和微调呢
> 后序展开

迁移学习时模型的网络结构是不是基本不变，除了输入层输出层？模型就像个黑匣子一样，针对这个黑匣子层级的修改是模型算法工程师做的工作吗
> 是的

两个汽车可以照搬，如果是卡车或者苹果，agent会重新训练吗？他是怎么判断传过来的是汽车呢？从哪一步确定是否使用迁移学习呢
> 深度学习 和 agent 很少有联系； agent 和 大模型；
> 图像分类， 图像分类

学习率是抽样的意思吗，损失是怎么产生的，如果都能算出loss，那就应该知道绝对正确的值？
>

那这些面试点后期会有一个整合吗？类似八股文这种死记硬背的~


迁移学习是程序员手动执行的还是已写好的模型自己实现的？
> 人工

mini-batch和batch-size是一个东西吗
> mini-batch 把样本汇总为一批训练； batch-size 是batch 的样本，一批样本中样本的个数

对于某个特定领域的任务而言，如何判断是使用迁移学习还是使用RAG的方式让大模型更胜任该特定领域？
>

深度学习的这些知识点感觉很难理解，后面会系统性的讲解深度学习吗？还是说需要自己去系统化学习


梯度是做什么的呢？不清空会有什么影响吗？
>

pytorch 验证过程：如何验证模型可用呢
>

梯度是怎么计算的？计算梯度是怎么用来更新模型参数的？

问 训练的是数据分布？生成就是根据训练的分布权重生成（不存在的）数据样本？
>

微调是根据梯度结果修改权重，然后超参数不变？
> 超参数 是人工设定的，并且不能自动调整；

梯度下降本质是调整模型的权重和偏置参数吗，以及梯度下降要到什么程度才能代表这个模型参数算是比较合理的
> acc /

deepseek 不是支持吗  设置 response_format 参数为 {'type': 'j
>

Agent就是大模型+harness
>

进行偏导计算梯度调整权重，怎么判断权重参数合理防止过拟合
> 验证集

既然有上下文学习，现在对话是写一个整体的markdown格式的文件存提示词微调，还是多轮对话效果好哇
> 大模型应用 是 避免微调

如何保证最终输出的会话（一句话）是通顺的，没有语法错误的

NNLM 和 深度学习 是什么关系
> NNLM 是一个特别的深度学习模型，神经语言模型

那bm25是进阶的词向量算法吗
> bm25 是 信息检索

词嵌入和bge有关系吗
> bge 是 bert模型，bert 的输入 使用了 word embedding
> bge 是 句子嵌入

word2vec是不是基本都用不到。gpt，bert都有embedding模型。也可以反向传播得到
> 面试用

多维词向量，是指将一个词做编码重排为一个一维矩阵，将这个词对应的多个分类的权重，存储在约定的位置吗
>

dropout是怎么选择在训练的时候放弃哪个神经元的 怎么保证恢复成标准网络后是没有问题的呀
> 随机选择

判别模型与生成模型 隐身到 序列模型之间的一个关系是 输入对结果影响的考量吗？
- 模型 可以分为 判别模型 或 生成模型
- 模型 可以分为 有监督模型 或 无监督模型 强化学习模型
- 模型 可以分为 非序列模型 或 序列

老师，代码我都能看懂，但是最近半年手写的少会有问题不
> 看懂 。 vibe coding

向量与向量之间的相似度是怎么算的，是通过哪种方法计算的

分布语义假设 对于每个词的不同维度的数值是怎么得到的

大模型的推理能力和大模型的预测能力是什么区别？和AI对话问答是属于大模型的推理还是预测？
> 大模型预测token ，大模型在预测过程中涌现出的高阶表现， 智能 / 推理 /类比

老师  怎么才能直观测试智能体是否改了提示词之后效果更好呢。在公司开发智能体的时候没办法量化
> 10条输入 -》 智能体 -》 输出表现；

长短记忆是如何区别实现的？短期会定期删除还是怎么样？
> 短期记忆，历史对话 / 上下文   -》 压缩 / 截断
> 长期记忆，知识库 （es文档数据库、向量数据库、图谱数据库） -》 检索到最相关的知识

agent通过什么途径实现感知？通过什么途径获取到反馈？
> 你可以定义agent

记忆在对话中会消耗token吗？使用智能体如何省token吗 我们会讲的很细吗 在token消耗这块
> 只要调用大模型 就会 消耗； 产出比；

skills是长期记忆么
> skill 是 可重复的agent技能，结合强agent框架使用，比如claude codex

老师可以介绍下agent的工作流区别，比如ReAct、Plan&Execute、Multi-Agent
> ReAct: Reason（推理） → Act（行动） → Observe（观察） → Reason（再推理）……形成一个循环。每一步行动前都要“想一想”，看到结果再“想下一步”。
> Plan&Execute: 先规划，然后行动
> Multi-Agent: 多个agent可以协调； 高代码 + 多agent应用 比较有价值

如果是0基础转行到agent岗位，万一过几年之后通用大模型可以替代agent了，还可以从事什么工作
> 成本，短期不太可能

agent不是相当于多个工作流构成，用大模型判断使用哪种工作流吗
>

根据输入去选择不同的大模型，是否就是常说的路由到xx大模型？
>
如果用户输入客服相关，则使用；

问 另外这种场景大模型是否都是在云端，不然本地部署占用的内存是不是太大了。
> Ai 应用 使用云端大模型更加方便； 本地部署一个大模型，通过远程调用即可；

能通过skill的方式输入给opencode，实现给多个LLM分发问题的需求吗？
> 能给你解决问题，不是企业落地的

检查代码的功能性/安全性这些一定要多次调用大模型么，能否一次性调用，有什么区别
>

codex是工作流还是agent呢？我看codex可以一直自己迭代思考行动
> codex 是 强agent

一个工作流，相同的输入，但输出经常不同，这个工作流是否不合格
> workflow / agent：

CoT在workflow起到了什么作用，和上下文的区别是什么呀
> CoT（Chain of Thought，思维链）：人工引导大模型思考，也可以直接调用推理大模型，让大模型自己思考

低代码的工作流像是rpa
> coze / dify 有 rpa 区别，rpa操作你自己本地电脑； coze 在云端搭建应用，在云端运行

老师， n8n是什么？
> n8n / coze / dify 都是低代码 agent 平台

老师，codex感觉可以做任何事，您觉得定制agent的意义在哪里。
> 10个用户需要进行文本翻译 -》 多次重复、比较规范的业务
> codex / cc 可以进行翻译， 随机性、不可控、 很难集成到后端

现在高代码Agent框架有很多，现在Langchain会不会过时，不用LangGraph吗
> LangGraph 使用 图 、 节点 编排应用，比较底层，写起来繁琐；

高代码工作流和智能体分别是怎样的
> 工作流 是 确定性的流程
> 智能体有自己的决策过程，选择执行 / tool / 存储记忆

我们会分析langchain的内部实现吗？
> 更加关注使用langchain

langchain_openai 第三方的实现，方便和langchain集成
openai 是 openai 官方实现，openai sdk

老师如果是云端智能体那么都是怎么做的环境隔离
> 容器隔离。每个容器部署一个智能体

老师，opencode这个工具是不是可以嵌入在ide里面？
> 可以的

model绑定工具，和langchain的agent绑定工具，有什么区别吗？

多个大模型之间可以复用同一个session，或者同一个上下文吗？
> 可以的

老师再详细介绍下现在大模型为什么不用RNN训练？
> 效率、效果、梯度消失/爆炸

问 老师今早讲的理论知识哪些是重点呢，都只是需要理解就行了吗，需要深入了解吗
> rnn的缺点， 词向量， 语言模型的发展

项目中有四个调用方式，实际用户的问题具体调用哪个方式怎么确定呢？还是说实际项目中保留一个就好
> 后序讲解

我们的课程是会讲解强智能体逻辑项目么，学完能开发出实际可用项目么？
> 关注解决业务实际问题，会用cc，cc各种功能即可；

老师，国内为什么没人做类似codex的软件出来
> opencode

面试的时候会问CC的源码或者架构吗？
> 会的


老师 我怎么才能测试大模型的能力 用哪些方式测试
> 测试案例， 功能测试 / 非功能测试 / 安全测试


作业1: 安装langchain 和 openai-agent，运行 langchain / 02_Model工具调用.py
    回答如下问题：
        1、 langchain 工具调用 和 llm function call 有什么区别？
        2、 langchain 工具调用 的 速度是受到什么影响？

作业2: 阅读意图识别 01-intent-classify 代码，梳理源文件的作用，绘制从fastapi 接受请求到
     到返回结果的流程，绘制流程图（手绘、自然语言表达）。

