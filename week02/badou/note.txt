streamlit：python写前端页面
torch： 深度学习框架，大模型底层都是用torch搭建 和 训练
sentence_transformers: 基于torch，快速加载和使用模型
openai： openai sdk ，大模型调用和返回结构
dotenv： 加载配置


pip install streamlit
(py312) lyz@192 Week02_2026Q2 % streamlit run src/streamlit_app.py
streamlit python 第三方库，让python写前端ui 前端逻辑

sklearn python 所有的机器学习方法 / 模型

本地机器（公司发一个笔记本电脑）：windows / Mac
    安装 codex ， cc ，pycharm  代码编写
预发环境（没有ui、阿里云、autodl）：linux -》 企业数据库
    验证代码
集成环境：linux
    集成代码
生产环境：linux
    部署项目

你是专业的知识总结助手，请结合相关的文档内容回答用户的提问：
用户提问： {user_query}
相关文档：
 - {top1_doc}
 - {top2_doc}
如果相关的文档和提问无关，则回答请重新提问。
如果没有检索到相关的文档，请回答不执行。

咋們會用到虛擬機麼
> docker 打包镜像，运行容器，类似虚拟机

这个需要部署本地大模型吗
> 会讲部署

大模型检索数据库和知识库的效率区别大吗？

问 知识库是否作为提示词输入到大模型中
> RAG 就是这个思路

知识库会占用上下文长度吗
> 送到llm 作为输入，就占用

为什么我们公司不准用云端大模型，调用云端大模型会导致泄密的原理是什么
> deepseek / gpt 会使用用户对话来训练模型

本地知识库，云端模型调用本地知识库，敏感数据不会被模型学习？
> 本地模型不会

百度上文心的深度思考是不是就属于外部知识库
> thinking 是 llm 的输出，不是

外部知识库和内部知识库可以同时搜索，交叉验证么
> 可以

第三方外部知识库会不会有收费的

本地部署大模型的回答结果是不是就不会用到外部知识库
> 知识库 和 llm 是不同的概念

外部知识库获得到的数据，需要存储吗？还是直接处理显示出来
> 可以

老师 如果想建自己的知识库 有啥好的方法和使用的软件推荐不？

如果我要搭建一个材质描述的知识库，是不是收集相关信息，一股脑json塞给ai就行了
> rag项目 中很多代码 -》 信息检索 / 检索信息 填充提示词 -》 如何将信息更加有效的进行组织为提示词
> 输入的信息更加让大模型困扰，大模型输出更差。

本地知识库也要考虑到信息敏感性问题吧，有的普通员工不能看的
> 权限管理，某些知识库只有某些人能看 / 能被检索

用户提问和爬虫的因果关系是什么？

老师 ‘业界哪种检索模型用的比较多呢
> es 中 讲解

中文的向量模型对比纯英文是不是误差会稍大一点，很多词语有多种意思
> 多种方法检索

不同模型的使用场景，会混合使用吗

streamlit不是做前端的麼

詞頻和重要性是正相關還是負相關
> tfidf bm25 讲解

通过文档找文档中的关键字，是正向索引；通过文档中的关键字找文档，是反向索引，这么理解对不？
> O（N）， O（1）

PDF怎么提取文本？是分页截图成PNG然后来提取文本嘛？
> mineru / paddlerocr 识别图片，格式

嵌入是在做反向排序吗

老师，嵌入模型每个公司应该都不一样的吧，主流分别都有哪些呢
> bge / qwen embdding / jina embedding

在划分chunk的时候，会不会把原来一段完好的句子拆分成不连贯的句子
> chunk overlap

跟直接发给豆包文件让他告诉我相关答案有什么区别呢老师？
>

不能一次性把企业的知识库一次性塞给大模型,让他自主学习吗?后面用户提问就做缓存命中
> 微调不建议

bge 文本是要分词吗？还是整个文档内容是一个文本？
> bge 是 bert 模型，bert模型不需要分词，

torch是什么？
> torch是深度学习框架，torch搭建大模型，微调大模型

codex 和 openai 模型调用有什么区别？
> openai sdk 是 通过代码调用大模型
> deepseek 官方， 通过ui 调用大模型
> codex 是 输入 进行修改，增加一些本地项目的上下文或相关信息，本质也是调用大模型

把搜索到的企业知识库都塞给大模型，是不是需要解决信息泄露风险呀
> 本地大模型不存在风险

文档编码成向量的这个逻辑我们不用懂是吗？
> 第三节课 第四节课， bert bge 模型；

老师，我们怎么去定要多少维度的，根据什么计算方式去定我们要存512维度还是更高维度的
> bge 作者 决定的

怎么计算相似度？为什么就得出相似度很匹配了
> cosine similarity

现在给企业做知识库智能客服，还有搞头嘛
> 有的

rag原理一样，那是不是使用一套现成的就可以啊
> 知识库不同，输入不同， 格式不同，要求不同，代码不同

阿里 禁用了cc,说是有后门,对个人有影响么?需要注意啥不
> cc / codex

现在好像纯做知识库已经不行了，得会agent记忆才行。像agent memory context7 honcho这种
> 后序讲解

知识库如何更新，不影响线上用户使用
> es 同时更新 + 检索；

所以机器学习模型包含深度学习模型，深度学习模型包含大语言模型？
> 是的

深度学习是不是就是没有特征提取这些前处理
> 是的， 深度学习可以自动提取特征

infra岗位的知识是哪些呀

> 框架和库有什么关系
框架 是 一个大的概念

数学基础不好，需要自己另外补习线性代数、微积分吗，面试会涉及吗
> 不需要

2022年说模型效果和模型参数、深度没太多关系，现在是算力上来了，所以模型效果和参数、深度有关吗
> 大模型 的scaling law： 算力、模型大小、计算量

我想问下 现在的模型在理解用户意图这件事情上肯定会做的越来越好，那这个提示词工程未来的发展方向是什么
> 模型能力 是通用能力； 大模型 + 提示词 -》 领域应用


choice[0]是第一轮的回答，choice[1]是第二轮回答，是这样吗
> 大模型结合输入可以回答多

除了openai，还有别的交互协议吗？是现在普遍都用openai的交互协议？
> openai / a社

阿里百炼api相关的调用是什么样的？


神經網絡的記憶  是靠權重保存的  那麼人的記憶也是記憶的權重麼
> 权重 保留 信息，大模型能力 通过权重保留

反向传播在预训练和后训练当中，根据这个 output 的一个情况，它这个很多参数它都是自己自动调的吗？它具体实现过程是怎么样的
> 模型训练 是 调节模型参数

totalTokens是多轮对话的总的消耗的token量吗 还是单次对话消耗的token的量
> 是这次请求的消耗，这次请求是需要历史对话的

本地部署的大模型有推荐吗？
> ollama ， vllm

什么叫512维度
> 数组 是 512个浮点数

老师，你说的那本学习深度学习的书叫什么啊
> https://zh.d2l.ai/   不是必须的

如何去写好提示词
> 结合提示词效果 和 速度，人工调整，工作内容之一

老师，我们后面还会学embbing层吗，还是只需要学会调用bge-small-zh-vl.5
> 会的

老师这些代码是完全底层的实现为了方便理解大模型吗？在些项目时需要写这些代码吗？
> turorial-llm-client 部分 代码 会写；


老师，深度学习可以自动提取特征，为什么现在企业里还是会先构建特征工程，然后再跑模型。
> 人工特征工程是要必要， 人工业务知识 更好的转换为输入；
> 深度学习的自动提取特征 需要 训练，并不一定比人工好；

现在的大模型之间的能力差距是主要是利用函数算法不同导致差距，还是算力差距？
> 模型能力的差异

老师，调用不同api，client.chat.completions.create里面参数是一致的吗
> 是的， qwen 、deepseek、 minmax、 kimi 都是一样
> 调用 claude 模型 则不是

思考我理解就是问大模型的时候说先把我这句话拆解计划，然后再根据拆解的计划做具体回答。不知道这么理解有
> 问题拆解、模型自我反馈； 后序讲解

在网页上使用deepseek之类的对话模型,也会把历史对话当做输入 反复提交么
> 是的

什么是特征提取？
> 原始输入进行转换

本地部署推荐部署哪些模型 例如千问模型 推荐部署哪些参数的 35B , 6B，0.6B, 例如RAG的提示词融合选择哪个参数的大模型更好

老师，如何提高RAG召回准确率，感觉有些召回虽然有关键词，但含义关联度不高
> rerank

max_tokens的设置值根据什么来判定
> 自己设置

上下文压缩，是有现成的算法吗？
> 主要方式让大模型总结历史对话，长文本 总结为 短文本

大模型是怎么知道什么时候该调用工具，什么时候是直接输出文本
> 模型训练的时候有对话训练样本，也有的tool call 训练样本

这个跟MCP有什么区别呢  听着好像
> tool 本质 mcp底层能力，最终工具选择还是tool call
> mcp 帮我组织这些工具，生成json，远程的工具调用

**args
> d = {"arg1": 1}  function(**d) =  function(arg = 1)

那这个tools的数据是不是需要自己爬虫，还是调用其他的API就行
> tool call 进行了 工具选择 + 参数生成

看上去和用skills一样，安装了多个skills时，大模型根据描述判断用哪个skill
> 核心是差不多

如果写了了tools，大模型就一定会调用工具吗
> 会结合工具描述选择

具体调用哪个第三方工具,也是大模型根据提问 自己选择的
> 待选的工具你定义的

怎么判断一次对话结束了呢，如果每次对话都加上之前的上下文内容，不是很浪费token吗

老师 是不是这些东西 什么语言其实都可以写
> openai 协议 都是http ，可以用其他语言；

toolcall基本用非流式输出吗？那一个问答agent，内置几个工具，用户问问题时，前端怎么展示的
> 选择了一个工具， 展示这部分信息
> 实际一个应用的思考过程，可以是自定义的；

将大模型本地化部署并根据本地知识库调试完了，如果现在的模型同样大小的情况下调优了，如何更好的切换呢？
> 切换模型 会 评测， 成本、速度、效果

还是不太理解response.choice[0]
> choice 大模型结合同一个输入进行了多次返回，消耗更好的token
> 帮我写一个博客，xxx主题 3个文章
> choice[0] choice[1] choice[2] 都是完整的博客内容，三个中选择一个比较好

json输出是为了更好的获取结果吗
> 结构化信息展示

tutorial-llm-client中的代码是不是需要自己能够熟练敲出来
> 代码不需要背。理解 + 需要用的时候能找到或知道用什么

一般都是把之前的对话持久化到对应的电脑，每次去拿吧。不可能一直放在内存中吧
> 数据库

大模型通过训练来选择工具是其本身具有的功能吗，我们是不是不需要了解
> 使用这个功能

有mcp和skills还为什么要用tool

直接让AI帮忙写提示词是不是就行了，零样本、少样本AI都知道吧
> llm 可以帮我们优化提示词，但是还是人工判断

把好的提示词结构框架直接放在skill里。直接调skill就可以了是吗
> 提示词 是 如何组织为llm的输出
> skill 是 agent 可以复用的能力

深度思考、思维链和模型参数设置的thinking有什么关系吗
> 思维链 是 提示词角度
> thinking 是 模型角度

提示词和我们用户输入的问题有啥区别？

大数据开发,想实现sql自动解析,执行等功能. 表信息,脚本等是作为知识库么? 然后写skill?
> agent实现方式，高代码实现 / 低代码实现 / skill


作业1: 本地安装streamlit，配置大模型，使用下四个页面
作业2: 借助于llm tool call 或 json mode 能力，构建一个简单的情况情感分析智能体。提交实现代码。

输入：小明喜欢小姚，但是小姚喜欢小王。
输出：人物关系图谱

[
    {
        "source": "小明",
        "relation": "爱慕",
        "target": "小姚"
    }
]


